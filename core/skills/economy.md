# Sextant Skill: Economy

Purpose: minimize implementation after correctness has been established.

Order of preference:
1. existing behavior/utility;
2. existing project abstraction;
3. platform or standard library capability;
4. smallest new local implementation;
5. new dependency or broad abstraction only when evidence requires it.

Economy is a final optimization stage, not a substitute for understanding the problem.

Output state:
MINIMAL_PLAN, AVOIDED_WORK.
